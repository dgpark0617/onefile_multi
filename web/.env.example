# 검신 — Vercel / Supabase (선택)
# 복사: cp .env.example .env.local

# 로컬만 쓰면 비워 두세요. 메모리 store 로 동작합니다.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# true 이면 이후 store 어댑터가 Supabase 를 쓰도록 전환 (아직 미구현 플래그)
GEOMSHIN_USE_SUPABASE=false
